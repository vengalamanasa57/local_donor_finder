name: local_donor_finder
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1

  firebase_core: ^4.6.0

  cloud_firestore: ^6.2.0
  
  google_maps_flutter: ^2.9.0
  
  geolocator: ^13.0.2
  
  geocoding: ^4.0.0
  
  cupertino_icons: ^1.0.8
  
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true

  
